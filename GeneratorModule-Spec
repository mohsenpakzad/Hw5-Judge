Generator function specification:

1- Takes input/output folder path as first argument and test code as second argument.
Both arguments are guaranteed to exist.(same for judge)
Input/output is also guaranteed to have equal ins and outs.

2- If there is compilation error it would show it on screen or if -L has been used write it to log.
Return error and end program
(Same function can be used in compiling for judge with minor modifications)

3- Folder structure: (deleted upon terminal exit)
- TestCase
  --In
    ---1.in
    ---2.in
  --Out
    ---1.out
    ---2.out
